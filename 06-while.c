#include <stdio.h>

int main() {
  int i = 20;
  while (i > 0) {
    printf("Itération %d\n", i);
    i--;
  }

  int j = 0;
  while (j < 20) {
    printf("Itération %d\n", j);
    j++;
  }

  return 0;
}
